apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-tank-operator
  namespace: mcp-tank-operator
spec:
  replicas: 1
  # Single-node cluster can't fit two replicas during a rolling-update surge.
  # Recreate keeps the rollout unstuck.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: mcp-tank-operator
  template:
    metadata:
      labels:
        app: mcp-tank-operator
    spec:
      serviceAccountName: mcp-tank-operator
      containers:
        - name: mcp-tank-operator
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: upstream
              containerPort: {{ .Values.upstreamPort }}
          env:
            - name: PORT
              value: "{{ .Values.upstreamPort }}"
            # In-cluster orchestrator URL for /api/internal/sessions/* calls.
            - name: ORCHESTRATOR_INTERNAL_URL
              value: "{{ .Values.orchestratorUrl }}"
            # Audience-scoped SA token for calling the orchestrator's
            # /api/internal/* endpoints. The token is minted for the
            # "tank-operator" audience and is separate from the default
            # Kubernetes API audience token.
            - name: TANK_OPERATOR_SA_TOKEN_PATH
              value: /var/run/secrets/tank-operator/token
          volumeMounts:
            - name: tank-operator-sa-token
              mountPath: /var/run/secrets/tank-operator
              readOnly: true
          readinessProbe:
            # Server binds 0.0.0.0:{{ .Values.upstreamPort }} and is reached
            # directly via the Service — no kube-rbac-proxy sidecar fronts it.
            httpGet:
              path: /healthz
              port: upstream
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: tank-operator-sa-token
          projected:
            sources:
              - serviceAccountToken:
                  audience: tank-operator
                  expirationSeconds: 3600
                  path: token
